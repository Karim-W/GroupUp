class request {
  String gID, cID, from, to, rID, msg, groupN, sender, courseName;
  request(this.rID, this.gID, this.cID, this.from, this.to, this.msg,
      this.sender, this.courseName, this.groupN);
}
